<div class="row">
  <div class="col-6">
<%= form_with(model: bottle) do |form| %>
  <% if bottle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bottle.errors.count, "error") %> prohibited this bottle from being saved:</h2>

      <ul>
        <% bottle.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :restaurant_id %>
    <%= form.number_field :restaurant_id %>
  </div>

  <% if @bottle.id %>
    <div class="mb-3">
      <%= form.label :status %>
      <%= form.text_field :status %>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit 'Keep the bottle' %>
  </div>
<% end %>
  </div>
</div>
